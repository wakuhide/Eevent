<%= form_for(@questionnaire) do |f| %>
  <% if @questionnaire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@questionnaire.errors.count, "error") %> prohibited this questionnaire from being saved:</h2>

      <ul>
      <% @questionnaire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cached_satisfaction_list %><br>
    <%= f.text_field :cached_satisfaction_list %>
  </div>
  <div class="field">
    <%= f.label :cached_trigger_list %><br>
    <%= f.text_field :cached_trigger_list %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :template %><br>
    <%= f.check_box :template %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
